language: java
jdk:
  - oraclejdk8
  - openjdk8
  
script:
  - cd Assignments/Records-Managment
  - ant compile jar test
  - cd ../Java-Rmi/Server
  - ant compile jar test
  - cd ../Client
  - ant compile jar
  - cd ..
  - ant -f Server run &
  - ant -f Client test
  - cd ../Corba/Server
  - start orbd -ORBInitialPort 1050 &
  - ant compile jar test
  - cd ../Client
  - ant compile jar
